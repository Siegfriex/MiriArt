/**
 * MiriArt Mock Data — Central Export
 * 모든 Mock 데이터는 이 파일을 통해 import합니다.
 *
 * 실제 API 연동 후에는 이 파일만 교체하면 됩니다.
 */

export {
  MOCK_ARTWORKS,
  AI_SUMMARIES,
  getRecentArtworks,
  getAllArtworks,
  getArtworkById,
} from './artworks';

export { MOCK_SESSIONS, getSessions } from './sessions';
